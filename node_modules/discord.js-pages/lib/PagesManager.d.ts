import { PagesBuilder } from './PagesBuilder';
import { Middleware, EndMethod, Action, ActionLabel } from './interfaces';
declare class PagesManager {
    get middleware(): Middleware;
}
export { PagesManager, PagesBuilder, EndMethod, Action, ActionLabel };
